[project]
name = "rl-arm-tracking"
version = "0.1.0"
description = "RL policy for 3D Cartesian end-effector trajectory tracking on a Franka Panda arm (Humanoid Robotics intern challenge submission)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
